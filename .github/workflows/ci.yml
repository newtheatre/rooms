# Lint + typecheck + build on every push and PR. Deployment is NOT here:
# Cloudflare's Workers Builds git integration deploys main on its own.
name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  check:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: read
    steps:
      - uses: actions/checkout@v4

      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest

      # GH_PACKAGES_TOKEN reads @newtheatre/auth-types from GitHub Packages (ADR-0025).
      - name: Install
        run: bun install --frozen-lockfile
        env:
          GH_PACKAGES_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Lint
        run: bun run lint

      - name: Comments
        run: bun run check:comments

      - name: Typecheck
        run: bun run typecheck

      - name: Build
        run: bun run build
